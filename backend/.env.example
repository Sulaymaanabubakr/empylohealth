# Copy to .env and fill in values. For Cloud Functions, set these in Firebase/Google Cloud env config.

# Cloudinary (media uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Daily.co (video huddles)
DAILY_API_KEY=

# Seed/backfill HTTP endpoints – must be set in production (no default)
SEED_TOKEN=

# Optional: restrict seed/contact origins (default *).
# SEED_ALLOWED_ORIGINS=https://your-admin.example.com
# CONTACT_ALLOWED_ORIGINS=https://your-landing.example.com

# Affirmation schedule timezone (default UTC)
# AFFIRMATION_TIMEZONE=Europe/London
